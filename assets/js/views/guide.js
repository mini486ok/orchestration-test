// 사용 가이드 — 플랫폼 개요, 사용 순서, Ollama 연결 설정
import { el } from '../core/ui.js';

const SECTIONS = [
  {
    title: '🎯 이 플랫폼은 무엇인가요?',
    html: `
      <p><b>Rail-Brain Test Lab</b>은 철도·교통 분야의 MCP(Model Context Protocol) 서버 기반
      <b>오케스트레이션 기술을 개발하고 검증</b>하기 위한 테스트 플랫폼입니다.</p>
      <ul>
        <li><b>MCP 카탈로그</b> — 테스트용 MCP 서버(description, input/output schema)를 등록·조회·삭제합니다. 기본으로 <b>10개 분야 × 10종 = 100개</b> 철도·교통 샘플 서버가 제공되며, 실제 서버 없이 스키마 기반 모의(Mock) 실행이 이뤄집니다.</li>
        <li><b>오케스트레이션 스튜디오</b> — <b>프롬프트 · 스킬 · 룰 · DB(vector/graph)</b> 4가지 방식으로 전략을 설계합니다.</li>
        <li><b>벤치마크 랩</b> — "질의 → 정답 워크플로우" 세트를 만듭니다. 기본 1개 + 분야별 10개 + 복합 시나리오 1개(총 12세트·140문항)를 제공하며, <b>한 질의에 여러 정답</b>(순서 무관·대안 워크플로우)을 인정합니다.</li>
        <li><b>평가·비교</b> — 여러 전략을 같은 벤치마크로 실행해 정확도·목표 달성·토큰 효율을 정량 비교하고, <b>오케스트레이션 종합점수</b>로 순위를 매깁니다.</li>
      </ul>`,
  },
  {
    title: '🚀 권장 사용 순서',
    html: `
      <ol>
        <li><b>설정</b>에서 Ollama 연결을 확인하고 기본 모델을 선택합니다. (기본값: exaone3.5:7.8b)</li>
        <li><b>MCP 카탈로그</b>에서 기본 제공되는 100개 샘플 MCP(10개 분야 × 10종)를 살펴보고, 필요하면 직접 만들거나 AI로 생성합니다.</li>
        <li><b>오케스트레이션</b>에서 전략을 만들고 테스트 콘솔에서 단건 질의로 동작을 확인합니다.</li>
        <li><b>벤치마크</b>에서 자동(LLM) 또는 수동으로 평가 세트를 만듭니다. 자동 생성 결과는 반드시 검토·수정하세요.</li>
        <li><b>평가·비교</b>에서 벤치마크 세트 + 전략 여러 개를 선택해 실행하고 결과를 비교합니다.</li>
      </ol>`,
  },
  {
    title: '🔌 Ollama 연결 설정 (중요)',
    html: `
      <p>이 앱은 브라우저에서 직접 로컬 Ollama(<code>http://localhost:11434</code>)를 호출합니다.
      GitHub Pages(https)에서 접속하는 경우 Ollama가 교차 출처 요청을 허용하도록 <b>OLLAMA_ORIGINS</b> 환경변수 설정이 필요합니다.</p>
      <p><b>Windows (PowerShell, 영구 설정):</b></p>
      <pre><code>[System.Environment]::SetEnvironmentVariable("OLLAMA_ORIGINS", "*", "User")
# 설정 후 작업표시줄 트레이에서 Ollama 종료 → 다시 실행</code></pre>
      <p>특정 출처만 허용하려면 <code>*</code> 대신 <code>https://mini486ok.github.io</code> 를 지정하세요.</p>
      <p><b>연결 확인:</b> 설정 화면의 "저장 후 연결 테스트" 버튼 또는 사이드바 하단의 상태 표시등을 확인하세요.</p>
      <p><b>Chrome "로컬 네트워크 액세스" 권한 (중요):</b> 최신 Chrome은 공개 사이트(https)가 localhost 등
      로컬 네트워크에 접근할 때 별도 권한을 요구합니다. 연결이 계속 실패하면 다음을 확인하세요.</p>
      <ol>
        <li>첫 연결 시도 시 <b>권한 프롬프트</b>가 뜨면 "허용"을 클릭합니다.</li>
        <li>프롬프트가 없었다면: 주소창 왼쪽 <b>자물쇠(사이트 정보) → 사이트 설정 → "로컬 네트워크 액세스"를 허용</b>으로 변경 후 새로고침.</li>
        <li>회사 관리 PC는 보안 정책/에이전트가 로컬 접근을 차단할 수 있습니다. 이 경우 아래 "로컬 실행"을 사용하세요.</li>
      </ol>
      <p><b>로컬 실행 (권한 문제가 있을 때의 확실한 대안):</b> 로컬에서 열면 localhost → localhost 통신이라 위 제약이 전혀 없습니다.</p>
      <pre><code>git clone https://github.com/mini486ok/orchestration-test.git
cd orchestration-test
python -m http.server 8000
# 브라우저에서 http://localhost:8000 접속</code></pre>
      <ul>
        <li>이 페이지를 <b>Ollama가 설치된 PC에서 열어야</b> 합니다. (브라우저 → localhost 호출 구조)</li>
        <li>다른 PC의 Ollama를 쓰려면 설정에서 주소를 바꿀 수 있으나, https 페이지에서 http 사설 IP 호출은 브라우저가 차단할 수 있습니다(mixed content). 이 경우 로컬 실행을 권장합니다.</li>
      </ul>`,
  },
  {
    title: '🖥 서버 모드 — 중앙 계정·쿼터·데이터 공유 (권장)',
    html: `
      <p>서버 PC(이 프로젝트의 <code>server/gateway.py</code>를 실행하는 PC)가 <b>중앙 게이트웨이</b>가 되어 팀 전체가 함께 씁니다.</p>
      <ul>
        <li><b>중앙 계정</b> — 계정을 서버에서 관리합니다. 서버에 계정이 있어야만 로그인·LLM 사용이 가능합니다.</li>
        <li><b>LLM 호출 쿼터</b> — 계정별 일일 호출 한도를 관리자(설정 화면)가 지정하며, 한도를 초과하면 LLM 호출이 거부됩니다. 남은 호출 수는 사이드바에 표시됩니다.</li>
        <li><b>데이터 공유</b> — MCP·전략·벤치마크가 서버에 저장되어 <b>모든 PC에서 동일하게</b> 보입니다(변경 시 자동 저장, 마지막 저장 우선 — 동시에 같은 항목을 편집하면 나중 저장이 이깁니다. 설정 → "지금 동기화"로 수동 갱신 가능). 평가 이력(runs)은 개인 브라우저에만 저장됩니다.</li>
      </ul>
      <p><b>서버 PC에서 (운영자):</b></p>
      <ol>
        <li><code>server/start-gateway.bat</code> 실행 (게이트웨이 기동, 포트 8799). <b>실행 창에 표시되는 "초기 설정 토큰"을 확인하세요.</b></li>
        <li><code>server/start-gateway-tunnel.bat</code> 실행 → 표시되는 <code>https://xxxx.trycloudflare.com</code> 주소 확보</li>
        <li>배포 페이지를 <code>…/?gateway=터널주소</code> 로 열면 서버 초기설정 화면이 뜹니다. 관리자 아이디·비밀번호와 함께 <b>1번의 초기 설정 토큰을 입력</b>해 최초 관리자 계정을 생성하세요.</li>
        <li>설정 → 게이트웨이 카드의 <b>"🔗 공유 링크 복사"</b>로 팀에 링크 공유, 계정·한도도 여기서 관리</li>
      </ol>
      <p><b>클라이언트 PC에서:</b> 공유받은 링크로 접속 → 서버 계정으로 로그인. 끝.</p>
      <p class="hint">💡 초기 설정 토큰은 터널을 거치면 모든 접속이 서버 로컬처럼 보이는 특성 때문에, 터널 주소를 아는 제3자가
      최초 관리자를 선점하지 못하도록 막는 장치입니다. 토큰은 <code>server/data/setup-token.txt</code>에 저장되어 재실행 시 유지되며,
      최초 관리자 생성 이후에는 더 이상 사용되지 않습니다. 여러 게이트웨이 창을 동시에 띄우면 포트 충돌이 나므로 한 번만 실행하세요.</p>
      <p class="hint">⚠ Quick Tunnel 주소는 터널 재시작 시 바뀝니다(고정 주소는 Cloudflare Named Tunnel 사용).
      게이트웨이가 인증을 강제하므로 터널 주소가 알려져도 계정 없이는 LLM을 쓸 수 없습니다.</p>`,
  },
  {
    title: '🌐 (대안) 서버 없이 LLM만 공유하기',
    html: `
      <p>중앙 계정·쿼터·데이터 공유가 필요 없다면, 게이트웨이 없이 Ollama만 노출할 수도 있습니다.
      이 경우 각 PC의 데이터는 각자 브라우저에 저장되고(공유 안 됨), LLM 호출 제한도 없습니다.</p>
      <p><b>방법 A. Ollama 직접 터널</b></p>
      <ol>
        <li><b>서버 PC:</b> <code>tools/start-ollama-tunnel.bat</code> 실행 → <code>https://xxxx.trycloudflare.com</code> 주소 확보.
          <code>OLLAMA_ORIGINS</code>에 <code>https://mini486ok.github.io</code> 설정 + Ollama 재시작 필요.</li>
        <li><b>클라이언트 PC:</b> <code>…/?ollama=터널주소</code> 링크로 접속(자동 설정) 또는 설정에서 직접 입력.</li>
      </ol>
      <p class="hint">⚠ 이 방식은 터널 주소를 아는 누구나 인증 없이 LLM을 호출할 수 있습니다.</p>
      <p><b>방법 B. 같은 네트워크(LAN) 직접 연결</b></p>
      <ol>
        <li><b>서버 PC:</b> <code>OLLAMA_HOST=0.0.0.0:11434</code> 환경변수 + Ollama 재시작, 방화벽 인바운드 허용(관리자 PowerShell):
          <pre><code>netsh advfirewall firewall add rule name="Ollama 11434" dir=in action=allow protocol=TCP localport=11434 profile=private,domain</code></pre></li>
        <li><b>클라이언트 PC:</b> 설정에서 주소를 <code>http://서버IP:11434</code>로 변경. Chrome/Edge의
          "로컬 네트워크 액세스" 권한 프롬프트를 허용합니다(브라우저·정책에 따라 차단될 수 있음 — 문제가 있으면 터널 방식 사용).</li>
      </ol>`,
  },
  {
    title: '📚 MCP 카탈로그 — 10개 분야 × 10종 (총 100개)',
    html: `
      <p>기본 제공 카탈로그는 한국 철도·교통 도메인을 <b>10개 분야</b>로 나누고, 각 분야마다 <b>10개</b> 서버를 담아
      <b>총 100개</b>의 샘플 MCP 서버로 구성됩니다(서버당 도구 2~4개). 실제 서버 없이 input/output 스키마 기반으로 모의 실행됩니다.</p>
      <ul>
        <li><b>운행정보</b> — 열차 시각표 · 실시간 위치 · 지연 감시 등</li>
        <li><b>예매·발권</b> — 예약 · 발권 · 정기권/패스 관리 등</li>
        <li><b>안전·관제</b> — 신호 제어 · 선로 안전 · 비상 출동 등</li>
        <li><b>시설·유지보수</b> — 선로 보수 · 자산 관리 · 전차선 전력 감시 등</li>
        <li><b>물류·화물</b> — 화물 추적 · 화물 예약 · 조차장 관리 등</li>
        <li><b>도시교통</b> — 지하철 길찾기 · 버스 환승 · 승강장 혼잡도 등</li>
        <li><b>여객서비스</b> — 여객 지원 · 유실물 · 역 안내 등</li>
        <li><b>기상·환경</b> — 철도 기상 · 대기질 · 재난 경보 등</li>
        <li><b>데이터분석</b> — 수송 실적 · 정시성 · 수요 예측 등</li>
        <li><b>요금·정산</b> — 요금 계산 · 환승 정산 · 결제 게이트웨이 등</li>
      </ul>
      <p class="hint">💡 도구들은 서로 <b>연결(io 체이닝)</b>되도록 설계되었습니다. 즉 한 도구의 <b>출력 필드 이름</b>이 다른 도구의
      <b>입력 파라미터 이름</b>과 맞아떨어져(예: 열차 검색의 출력 <code>trainNo</code> → 예약의 입력 <code>trainNo</code>),
      "검색 → 예약 → 발권" 같은 다단계 워크플로우가 자연스럽게 성립합니다. 카탈로그가 100개로 커진 만큼,
      질의와 무관한 도구를 걸러 주는 <b>DB 전략</b>(아래)의 효과가 특히 커집니다.</p>`,
  },
  {
    title: '🧪 벤치마크 — 분야별 세트 · 다중 정답 · 세트 조합',
    html: `
      <p>벤치마크는 "질의 → 정답 워크플로우"의 모음입니다. 기본으로 <b>12개 세트(총 140문항)</b>가 제공됩니다:
      혼합 문항의 <b>기본 검증 세트 1개</b> + 위 10개 분야별 <b>검증 세트 10개</b>(각 10문항) +
      <b>복합 시나리오 검증 세트 1개(30문항)</b>.</p>
      <p><b>복합 시나리오 세트</b>는 모든 문항이 <b>서로 다른 MCP 서버 3개 이상(3~5단계)</b>을 조합해야 풀리는 분야 교차 질의입니다
      (예: 기상 규제 확인 → 운행 조회 → 예매 환불, 센서 이상 → 작업지시 → 부품 예약). 서버 100종 중 정확한 조합을 찾아야 하므로
      <b>프롬프트 전략(전체 카탈로그 주입)과 DB 전략(검색 축소)의 변별</b>에 특히 유용합니다.</p>
      <p><b>한 질의에 여러 정답을 인정합니다(다중 정답).</b> 현실의 워크플로우는 정답이 하나로 딱 떨어지지 않기 때문입니다.</p>
      <ul>
        <li><b>순서 무관(ordered:false)</b> — 서로 독립적인 병렬 호출이라 순서가 상관없을 때, 순서가 달라도 정답으로 인정합니다.</li>
        <li><b>대안 워크플로우(alternatives)</b> — 서로 다른 서버·도구 조합으로도 목적을 달성할 수 있을 때, 본 정답과 대안 중 <b>F1이 가장 높은 것</b>으로 채점합니다.
          대안마다 <b>목표 도구를 따로 지정</b>할 수도 있습니다(<code>{"steps":[...], "goal":{...}}</code> 객체 형태) — 대안이 채택되면 그 대안의 goal로 목표 달성률을 채점합니다.</li>
        <li><b>목표 도구(goal)</b> — "결국 이 도구까지 성공해야 일을 끝낸 것"이라는 최종 목표 도구를 지정합니다. 지정하지 않으면 정답 시퀀스의 <b>마지막 도구</b>가 목표가 됩니다(목표 달성률 지표의 기준). 단, <b>순서 무관(ordered:false)</b> 항목은 마지막 도구 대신 <b>정답 도구 전부</b>를 오류 없이 완수해야 달성으로 인정합니다.</li>
      </ul>
      <p><b>세트 조합</b> — 벤치마크 랩의 "세트 조합" 기능으로 <b>기존 세트를 여러 개 골라 새 세트를 만들 수 있습니다.</b>
      각 세트에서 포함할 문항을 전체/일부로 선택하고, <b>중복 문항 제거</b> 옵션으로 겹치는 문항을 정리한 뒤 새 이름으로 저장합니다
      (예: 여러 분야 세트에서 hard 문항만 모아 "종합 난이도 세트" 구성).
      중복 판정 기준은 <b>"질의(정규화) 또는 도구+파라미터 시퀀스가 완전히 일치"</b>입니다 —
      같은 도구 순서라도 <b>파라미터가 다르면 서로 다른 문항으로 보존</b>됩니다.</p>
      <p class="hint">⚠ 자동 생성(LLM) 벤치마크의 정답은 오류를 포함할 수 있으니 <b>반드시 사람이 검토·수정</b>한 뒤 평가에 쓰세요.</p>`,
  },
  {
    title: '🧠 오케스트레이션 전략 4가지',
    html: `
      <p>전략 타입은 <b>프롬프트 · 스킬 · 룰 · DB</b> 4종입니다. 아래 3종을 먼저 소개하고, 4번째 <b>DB 전략</b>은 다음 절에서 자세히 다룹니다.</p>
      <ul>
        <li><b>프롬프트 엔지니어링</b> — 시스템 프롬프트를 직접 설계합니다. <code>{{TOOL_CATALOG}}</code>(등록된 도구 목록), <code>{{QUERY}}</code>(사용자 질의), <code>{{DATE}}</code> 플레이스홀더를 지원합니다.
        실행 모드는 <b>플랜 우선</b>(한 번에 전체 계획 수립 후 실행)과 <b>ReAct</b>(단계마다 결과를 보고 다음 행동 결정) 중 선택합니다.</li>
        <li><b>스킬 기반</b> — 자주 쓰는 작업 흐름을 "스킬"(트리거 설명 + 단계 시퀀스)로 정의하면, LLM이 질의에 맞는 스킬을 선택해 실행합니다. 단계 파라미터는 LLM 채움 또는 템플릿(<code>{{QUERY}}</code>, <code>{{step1.output.필드}}</code>) 방식을 지원합니다.</li>
        <li><b>룰 기반</b> — 키워드/정규식 조건과 워크플로우를 매핑합니다. LLM 없이 결정적으로 동작하며, 매칭 실패 시 오류 처리 또는 LLM 폴백을 선택할 수 있습니다.</li>
        <li><b>DB (vector/graph)</b> — 카탈로그를 별도 db로 구축해 질의와 관련된 도구만 플래너에 공급합니다. <b>바로 아래 "🗄️ DB 전략" 절</b>에서 자세히 설명합니다.</li>
      </ul>`,
  },
  {
    title: '🗄️ DB 전략 — vector db / graph db 로 카탈로그 DB화',
    html: `
      <p>전략 타입은 <b>프롬프트 · 스킬 · 룰 · DB</b> 4종입니다. <b>DB 전략</b>은 등록된 MCP 카탈로그를
      별도의 db로 구축해 두고, <b>질의와 관련된 도구만 골라 플래너(LLM)에 공급</b>합니다(전체 카탈로그를 프롬프트에 넣는
      낭비를 줄여 컨텍스트 효율과 도구 선택 정확도를 높입니다). db 종류로 <b>vector db</b> 또는 <b>graph db</b>를 선택합니다.
      샘플 전략 <b>"벡터 DB 플래너"</b>, <b>"그래프 DB 플래너"</b>가 기본 제공됩니다.</p>
      <p><b>① Vector DB</b> — 도구별 설명 텍스트를 임베딩 모델(기본 <code>bge-m3</code>)로 벡터화한 인덱스. <b>도구당 벡터 1개</b>입니다.</p>
      <ul>
        <li><b>검색 방식</b>: <code>vector</code>(임베딩 코사인) / <code>keyword</code>(BM25, 임베딩 불필요) / <code>hybrid</code>(가중합 α).
          top-K·임계값·확장(같은 서버/카테고리 이웃)·임베딩 모델을 편집기에서 조절합니다.</li>
        <li><b>임베딩 문서 구성(docFields)</b> — 각 도구를 벡터로 만들 때 문서에 담을 요소를 토글합니다:
          <b>설명</b>(도구/서버 설명) · <b>파라미터</b>(입력 설명) · <b>출력 스키마</b> · <b>예시</b>(examples/mock 값) · <b>태그</b>.
          기본값은 <b>설명+파라미터</b>이며, 넣는 요소를 바꾸면 검색이 잡아내는 도구가 달라집니다.
          <b>문서 구성을 바꾸면 벡터를 다시 계산해야 하므로 재색인이 필요합니다.</b></li>
        <li><b>MMR 다양성(λ)</b> — 검색 결과가 서로 비슷한 도구로만 채워지지 않도록 <b>다양성</b>을 조절합니다.
          <code>λ=1.0</code>이면 순수 관련도순(기본, MMR 꺼짐)이고, 값을 낮출수록 이미 뽑힌 도구와 겹치지 않는 도구를 우선합니다.
          MMR은 <b>검색 시점에만 적용</b>되므로 재색인이 필요 없습니다.</li>
      </ul>
      <p class="hint">📌 <b>청킹(문서 분할)은 도입하지 않습니다.</b> 도구 설명은 짧아서 여러 조각으로 쪼갤 실익이 거의 없기 때문입니다(도구당 벡터 1개 유지).
      대신 위의 <b>문서 구성 토글</b>과 <b>MMR 다양성</b>으로 검색을 다양하게 실험하세요.</p>
      <p><b>② Graph DB</b> — 도구를 <b>노드</b>, 도구 간 관계를 <b>엣지</b>로 하는 그래프. 질의로 시드 도구를 고른 뒤
      그래프를 순회(hop)해 연관 도구를 함께 공급합니다. <b>엣지 유형·가중치·임계값·hop 수를 편집기에서 자유롭게 조절</b>하며 테스트합니다.</p>
      <ul>
        <li><b>엣지 6종</b>(각각 on/off·가중치·임계값 조절):
          <code>io</code>(도구 A 출력 필드 → 도구 B 입력, 워크플로우 체이닝) ·
          <code>semantic</code>(임베딩 유사) · <code>server</code>(동일 서버) · <code>category</code>(동일 분야) ·
          <code>cooccur</code>(벤치마크 정답에서 공출현) · <code>llm</code>(LLM이 도구의 소비/생산 개념을 추출해 연결).</li>
        <li><b>llm 엣지</b>는 그래프 구축 시 도구당 1회 LLM을 호출하므로 <b>시간이 걸려 기본 off</b>입니다. 켜면 구축 시 실행됩니다.</li>
        <li><b>모델 선택</b> — 임베딩 모델과 llm 추출 모델을 각각 설치된 모델 중에서 드롭다운으로 선택합니다(기본값 고정이 아님).</li>
        <li><b>노드 최대 연결(maxDegree, 기본 12)</b> — 한 노드가 가질 수 있는 엣지 수의 상한(방향 io 출력 상한 포함, 슬라이더 4~30).
          값을 키우면 더 촘촘히, 줄이면 핵심 연결만 남깁니다. 상한을 <b>줄이는 것은 즉시</b> 반영되지만, 저장된 상한보다 <b>키우려면 재구축</b>이 필요합니다.</li>
        <li><b>허브 정규화(hubNorm, 기본 on)</b> — 그래프 확산 시 연결이 아주 많은 "허브" 도구가 결과를 독식하지 않도록 기여를 <code>1/√(연결 수)</code>로 완화합니다. 끄면 원래 가중치를 그대로 씁니다.</li>
        <li><b>경로 추천 파라미터</b> — io 엣지를 따라 다단계 워크플로우를 자동 추천할 때 조절합니다:
          <b>빔 폭(beamWidth 1~20)</b> = 동시에 탐색할 후보 경로 수, <b>최대 길이(maxLen 2~6)</b> = 경로의 최대 단계 수,
          <b>사용 엣지</b> = <code>io</code>만 또는 <code>io+llm</code>(llm 엣지가 켜져 있을 때만 선택 가능).</li>
        <li><b>시각화·미리보기·경로 추천</b> — 엣지 파라미터를 바꾸면 그래프가 즉시 다시 그려지고, 질의로 선택되는 도구·경로를 미리 확인하며,
          io 엣지를 따라 다단계 워크플로우(예: 열차검색→예약→발권)를 자동 추천합니다.</li>
        <li><b>호버 툴팁</b> — 그래프에서 <b>노드에 마우스를 올리면</b> 아이콘·서버 이름·도구명·분야·연결 수가, <b>엣지에 올리면</b>
          엣지 유형·의미·방향(A 도구 → B 도구)·가중치가 오버레이로 표시됩니다. 모달 확대뷰에서도 동일합니다.</li>
      </ul>
      <p class="hint">파라미터 기본값은 일반적이면서 시간효율적인 값으로 맞춰져 있고(무거운 llm 엣지·과밀한 category는 기본 off), 모두 조절할 수 있습니다.
      검색 결과가 0개거나 db가 없으면 자동 폴백합니다(트레이스 경고). 임베딩·그래프 구축의 LLM/임베딩 호출은 전략 실행의 LLM 호출 수에 포함되지 않습니다.
      <b>평가·비교</b> 화면에서 전체 카탈로그 전략과 vector/graph DB 전략을 나란히 돌려 정확도(F1)와 효율을 직접 비교해 보세요.
      특히 프롬프트 전략은 <b>모든 도구 정보를 프롬프트에 넣어 토큰이 많고</b>, DB 전략은 <b>검색으로 소수 도구만 넣어 토큰이 적으므로</b>,
      <b>토큰 지표·종합점수</b>에서 그 차이가 드러납니다.</p>`,
  },
  {
    title: '⚡ 실시간 테스트 — 전략을 골라 즉석에서 질의·비교',
    html: `
      <p>사이드바의 <b>실시간 테스트</b> 화면에서, 구축한 전략들을 <b>여러 개 선택</b>해 같은 질의를 던지고
      각 전략의 답변·호출한 워크플로우·실행 추적을 <b>나란히 비교</b>할 수 있습니다.</p>
      <ul>
        <li>전략을 체크로 여러 개 고르고 질의를 입력하면, 선택된 전략들이 <b>동시에 실행</b>되어 카드로 비교됩니다.</li>
        <li>각 카드에 성공/부분오류/실패 상태, 호출된 도구 워크플로우, 최종 답변, LLM 호출 수·지연, 접이식 실행 추적이 표시됩니다.</li>
        <li>질의를 이어서 던지면 대화 히스토리로 쌓이며, 각 질의 결과를 재실행하거나 JSON으로 내보낼 수 있습니다.</li>
      </ul>
      <p><b>벤치마크 대조 모드</b> — 상단 토글로 <b>"자유 질의 | 벤치마크 문항"</b>을 전환합니다. 벤치마크 문항 모드에서는
      세트·문항을 골라 질의를 자동으로 채우고 <b>기대 워크플로우·목표 도구</b>를 함께 보여 줍니다. 실행하면 각 전략 카드에
      정답과 대조한 <b>채점 결과</b>(F1 · 목표 달성 · 도구 성공률 · 토큰 · 종합점수)와 누락/잉여 도구 칩이 표시됩니다.
      다중 정답(순서 무관·대안)도 그대로 반영됩니다. 자유 질의 모드에는 정답이 없으므로 채점 없이 비교만 합니다.</p>
      <p class="hint">정식 정량 비교(P/R/F1 등)는 <b>평가·비교</b> 화면을, 빠른 즉석 확인·시연·문항 단위 대조는 이 <b>실시간 테스트</b>를 사용하세요.</p>`,
  },
  {
    title: '📏 평가 지표 · 오케스트레이션 종합점수',
    html: `
      <p>평가·비교 화면은 여러 지표로 전략을 채점하고, 이를 하나로 묶은 <b>오케스트레이션 종합점수</b>로 순위를 매깁니다.
      MCP 실행은 모의(Mock)이므로 채점 기준은 "<b>어떤 도구를 어떤 순서·파라미터로 호출했는가</b>"입니다.
      (각 지표는 평가 화면의 "📖 지표 설명(용어집)" 패널과 컬럼 헤더 툴팁에서도 같은 문구로 확인할 수 있습니다.)</p>
      <p><b>■ 헤드라인 — 오케스트레이션 종합점수</b> (높을수록 좋음)</p>
      <pre><code>종합점수 = 0.85 × 품질점수 + 0.15 × 토큰효율
품질점수 = 0.4 × F1 + 0.3 × 목표달성 + 0.15 × 도구성공률 + 0.15 × 파라미터정확도</code></pre>
      <ul>
        <li><b>품질점수</b> — 토큰을 빼고 오직 워크플로우가 얼마나 정확했는지만 본 점수. 구성 지표가 N/A(예: 도구 미호출·목표 특정 불가)이면 그 가중치를 빼고 <b>남은 가중치로 재정규화</b>합니다.</li>
        <li><b>토큰효율</b> — 함께 평가한 전략들 중 토큰을 <b>적게 쓸수록 높은 비율 기반</b> 상대 지표. 가장 적게 쓴 전략이 1이고, 그 전략 대비 토큰을 많이 쓸수록 낮아집니다.
          계산식 <code>가장 적은 평균총토큰 / 이 전략의 평균총토큰</code>. 함께 평가한 전략 집합에 상대적이라 <b>run 간 비교는 불가</b>하며, 단일 전략만 평가하면 1입니다.</li>
      </ul>
      <p><b>■ 도구 선택 정확도</b> (모두 높을수록 좋음)</p>
      <ul>
        <li><b>F1</b> — 호출한 도구 집합이 정답 집합과 얼마나 겹치는지의 종합 점수. <code>2 × 정밀도 × 재현율 / (정밀도 + 재현율)</code>.</li>
        <li><b>정밀도(Precision)</b> — 내가 호출한 도구 중 정답에 있던 비율. 낮으면 쓸데없는 도구를 많이 불렀다는 뜻.</li>
        <li><b>재현율(Recall)</b> — 정답 도구 중 내가 실제로 호출한 비율. 낮으면 필요한 도구를 빠뜨렸다는 뜻.</li>
      </ul>
      <p><b>■ 순서 · 파라미터 · 목표</b></p>
      <ul>
        <li><b>시퀀스 정확도</b>(높을수록 좋음) — 도구를 부른 순서가 기대 순서와 얼마나 맞는지(최장 공통 부분수열 기반). 순서 무관 문항에서는 큰 의미가 없습니다.</li>
        <li><b>완전일치율</b>(높을수록 좋음) — 도구·순서가 정답과 완전히 똑같이 실행된 문항의 비율.</li>
        <li><b>파라미터 정확도</b>(높을수록 좋음) — 도구에 넘긴 인자(from·date 등)가 정답 파라미터와 얼마나 일치하는지. 도구는 맞아도 인자가 틀리면 낮아집니다.</li>
        <li><b>목표 달성률</b>(높을수록 좋음) — 기대 워크플로우의 <b>최종(목표) 도구</b>를 <b>오류 없이 호출</b>(파라미터가 지정된 경우 절반 이상 일치)한 문항의 비율. "일을 실제로 끝냈는가"를 봅니다. 목표를 특정할 수 없는 문항은 <b>N/A</b>로 제외합니다.</li>
      </ul>
      <p><b>■ 도구 호출 품질 (신규)</b></p>
      <ul>
        <li><b>도구 호출 성공률</b>(높을수록 좋음) — 호출한 도구 중 입력 스키마 검증·실행에 성공한 비율 <code>(전체 호출 − 실패 호출)/전체 호출</code>. 낮으면 파라미터를 잘못 만들어 호출이 실패했다는 뜻.</li>
        <li><b>잉여 도구 호출률</b>(낮을수록 좋음) — 정답에 없는 불필요한 도구를 부른 비율. <code>1 − 정밀도</code>.</li>
      </ul>
      <p><b>■ 토큰 사용량 (신규, 낮을수록 좋음)</b></p>
      <ul>
        <li><b>입력 토큰</b> — LLM에 보낸 프롬프트의 토큰 수(<code>prompt_eval_count</code> 합). 프롬프트·컨텍스트가 길수록 큽니다.</li>
        <li><b>출력 토큰</b> — LLM이 생성한 응답의 토큰 수(<code>eval_count</code> 합).</li>
        <li><b>총 토큰</b> — 문항당 입력+출력의 평균. 적을수록 비용·지연이 낮습니다. 서버가 실측값을 주지 않아 글자수(≈ 글자수/2.2)로 추정한 경우 <b>≈(추정)</b> 배지가 붙습니다.</li>
      </ul>
      <p><b>■ 효율성</b> — <b>평균 지연</b>(문항 1개 처리 시간)과 <b>LLM 호출 수</b>(문항당 평균 호출 횟수, react류일수록 많음)는 낮을수록 좋습니다.</p>
      <p><b>■ 사전 점검 · 신뢰도 뱃지 (신규)</b></p>
      <ul>
        <li><b>사전 점검(실행 전)</b> — 평가 실행 설정 카드가 선택한 전략·세트를 기준으로 실행 전에 자동 점검해 <b>비차단 경고</b>를 띄웁니다:
          ① 전체 카탈로그 주입 전략의 <b>카탈로그 추정 토큰이 num_ctx를 초과</b>하는지,
          ② DB 전략의 <b>인덱스/그래프가 미구축·오래됨</b>(폴백으로 평가됨)인지,
          ③ ReAct 계열에서 <b>세트의 최대 단계 수 대비 maxSteps 여유가 부족</b>한지. 경고가 떠도 실행은 가능하지만 결과 해석에 반영하세요.</li>
        <li><b>신뢰도 뱃지(결과)</b> — 리더보드 행에 두 가지 경고 뱃지가 붙을 수 있습니다. 용어집 기준 의미는 다음과 같습니다:
          <b>컨텍스트 초과 문항 수</b>(⚠ctx) — 프롬프트가 numCtx를 넘어 잘렸을 가능성. 해당 전략 점수는 신뢰 불가.
          <b>검색 폴백 문항 수</b>(⚠폴백) — DB 전략이 의도한 검색이 아닌 폴백으로 실행됨(폴백 사유는 문항 상세·트레이스에서 확인).</li>
      </ul>
      <p class="hint">💡 <b>프롬프트 전략과 DB 전략의 토큰 차이가 핵심입니다.</b> 프롬프트·스킬 전략은 <b>모든 MCP 정보를 프롬프트에 넣기</b> 때문에
      입력 토큰이 많고, 카탈로그가 100개처럼 매우 커지면 프롬프트에 다 담지 못할 수도 있습니다. 반면 DB(vector/graph) 전략은
      <b>검색으로 소수 도구만</b> 넣어 토큰이 적습니다. 이 차이는 <b>토큰 지표</b>와, 토큰효율을 15% 반영한 <b>종합점수</b>에서 확인할 수 있습니다.
      전략을 비교할 때는 모델·온도를 동일하게 맞추세요(평가 실행 옵션).</p>`,
  },
  {
    title: '⚠ 평가 결과 해석 주의사항',
    html: `
      <p>이 플랫폼은 <b>MCP 실행을 모의(Mock)</b>하는 환경입니다. 아래 한계를 이해하고 결과를 해석하세요.</p>
      <ul>
        <li><b>관찰 기반 적응은 공정 비교 대상이 아님</b> — 모의 실행 출력은 스키마 기반의 (결정적) 랜덤 값입니다. 따라서 "관찰 결과에 따라 분기"하는 능력은 실제 데이터가 없어 제대로 평가되지 않습니다. <b>ReAct류 전략의 관찰 기반 적응은 이 환경에서 이점을 발휘하기 어렵습니다.</b></li>
        <li><b>자동 생성 벤치마크의 정답은 미검증</b> — LLM이 만든 "질의→정답 워크플로우"는 오류를 포함할 수 있습니다. <b>반드시 사람이 검토·수정한 뒤</b> 평가에 사용하세요(생성 후 검토 화면의 경고 참고).</li>
        <li><b>룰 전략의 LLM 폴백은 분리 해석</b> — 룰 전략이 매칭 실패로 LLM 폴백을 탄 항목은 사실상 프롬프트 전략에 가깝습니다. 리더보드는 <b>폴백 비율과 함께</b> 나눠서 해석하세요.</li>
        <li><b>스킬 전략의 precision 하락</b> — 스킬은 선택된 스킬의 <b>정의된 단계 전체를 실행</b>하므로, 사용자가 일부만 원한 질의(부분 의도)에서는 불필요한 도구 호출로 precision이 낮아질 수 있습니다. <b>F1과 Precision/Recall을 함께</b> 보세요.</li>
        <li><b>비교 조건 통일</b> — 전략을 비교할 때는 <b>모델·온도·maxSteps를 동일</b>하게 맞추세요(평가 실행 옵션 활용). 특히 ReAct는 여러 번 LLM을 호출하므로 <b>컨텍스트 길이(설정의 num_ctx)</b>에 민감합니다.</li>
        <li><b>ReAct의 maxSteps 여유 확인</b> — ReAct는 도구 호출 단계에 더해 <b>마지막 final_answer 생성까지 단계로 소비</b>하므로,
          N단계 문항에는 maxSteps가 최소 <b>N+1</b> 필요합니다. 특히 <b>복합 시나리오 세트는 5단계 문항이 13개</b>라 기본 maxSteps 6으로는
          여유가 전혀 없습니다(5+1=6). ReAct 계열로 복합 세트를 평가할 때는 maxSteps를 상향(전략 설정 또는 평가 실행 옵션의 maxSteps 통일)하세요.
          실행 전 사전 점검 패널이 여유 부족을 경고해 줍니다.</li>
        <li><b>LLM 타임아웃</b> — 설정의 <b>LLM 타임아웃(기본 300초, 0=무제한)</b>이 LLM 호출당 적용되어, 특정 문항에서 모델 응답이
          멈춰도 평가 전체가 멎지 않고 해당 호출을 오류로 처리한 뒤 다음으로 진행합니다. 느린 모델·긴 프롬프트에서 타임아웃이 자주 나면 값을 올리세요.</li>
        <li><b>컨텍스트 길이 확인</b> — 기본 <b>100개 서버(도구 수백 개)</b>의 카탈로그를 전부 프롬프트에 넣으면 수만 토큰에 달합니다. 기본값 <code>num_ctx 16384</code>로는 전체 카탈로그 전략의 프롬프트가 예산을 넘길 수 있으니 상향하거나, <b>DB 전략</b>으로 도구를 선별해 넣으세요. 프롬프트가 예산을 초과하면 실행 트레이스에 ⚠ 경고가 표시됩니다.</li>
        <li><b>순서무관·대안·목표 정답</b> — 항목 편집기에서 "순서 무관 채점"을 켤 수 있고, 대안 정답은 세트 JSON 가져오기로 지정합니다: 항목에 <code>"alternatives": [[{"serverId":"...","toolName":"..."}]]</code> 배열을 추가하면 본 정답과 대안 중 F1이 최대인 것으로 채점됩니다. 대안 항목은 배열 대신 <code>{"steps":[...], "goal":{"serverId":"...","toolName":"..."}}</code> <b>객체 형태</b>도 지원하며, 그 대안이 채택되면 대안의 goal로 목표 달성률을 채점합니다. 또 항목에 <code>"goal": {"serverId":"...","toolName":"..."}</code>를 지정하면 그 도구를 <b>목표 도구</b>로 삼아 목표 달성률을 채점합니다(생략 시 정답 시퀀스의 마지막 도구, 단 <b>순서 무관(ordered:false)</b> 항목은 정답 도구 전부 완수).</li>
        <li><b>DB 전략의 F1은 "검색 × 플래너" 결합</b> — DB 전략은 db로 <b>검색한 도구만</b> 플래너에 공급하므로, 검색이 정답 도구를 놓치면(recall 저하) 플래너가 아무리 좋아도 F1이 낮아집니다. 낮은 F1을 곧바로 "플래너가 나쁘다"로 해석하지 말고, 실행 트레이스의 <b>"DB 검색"</b>에서 어떤 도구가 선별됐는지 확인하세요(검색 파라미터를 조절해 recall을 높일 수 있습니다).</li>
        <li><b>그래프 db의 cooccur 엣지는 정보 누출 주의</b> — cooccur(벤치마크 공출현) 엣지는 <b>벤치마크 정답으로 그래프를 만들기</b> 때문에, <b>같은 벤치마크로 평가하면</b> 정답 도구끼리 직접 연결되어 성능이 부풀 수 있습니다(누출). 공정한 비교를 원하면 cooccur를 끄거나(기본 off), 그래프 구축용과 평가용 벤치마크 세트를 분리하세요.</li>
        <li><b>semantic·llm 엣지는 켠 뒤 재구축 필요</b> — 그래프의 후보 엣지는 <b>구축 시점에</b> 계산됩니다. semantic(임베딩)·llm(추출) 엣지를 새로 켜거나 임베딩/추출 모델을 바꾸면 <b>그래프를 다시 구축</b>해야 반영됩니다(이미 구축된 후보 엣지의 on/off·가중치·임계값 조절은 재구축 없이 즉시 반영). 상태 카드의 "재구축 필요" 안내를 참고하세요.</li>
      </ul>`,
  },
  {
    title: '🔐 계정·데이터 저장 위치',
    html: `
      <p><b>로컬 모드</b>에서는 계정과 모든 데이터가 <b>이 브라우저의 localStorage</b>에만 저장됩니다. 서버로 전송되지 않으며 repo에도 포함되지 않습니다.</p>
      <p><b>서버 모드</b>에서는 MCP·전략·벤치마크가 서버에 저장되어 모든 PC에서 공유되며, 평가 이력(runs)은 개인 브라우저에만 저장됩니다.</p>
      <ul>
        <li>다른 브라우저/PC에서 이어서 쓰려면 설정 → "데이터 내보내기"로 백업 후 가져오기 하세요.</li>
        <li>정적 호스팅 특성상 클라이언트 측 접근 제어입니다. 민감한 정보는 저장하지 마세요.</li>
      </ul>`,
  },
];

export function render(container) {
  container.replaceChildren(
    el('div', { class: 'stack', style: { maxWidth: '860px' } },
      SECTIONS.map(s => {
        const body = el('div', { class: 'guide-md' });
        body.innerHTML = s.html; // 정적 가이드 콘텐츠(사용자 입력 아님)
        return el('div', { class: 'card' }, el('div', { class: 'panel-title' }, s.title), body);
      })));
}
