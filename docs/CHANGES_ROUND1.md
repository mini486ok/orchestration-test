# Round-1 fixes applied (for round-2 reviewers)

1. orchestrator.js: step errors now degrade result.ok=false for plan/skill/rule (react exempt — errors feed back as observations); hasStepErrors flag on all paths; react failing when maxSteps reached without final_answer; {{stepN.output.x}}/{{QUERY}} template resolution now also applied in plan/react params; llmCalls counts actual HTTP calls (chatJSON retry = 2); usedFallback flag when rule strategy falls back to LLM; abort signal passed to executeTool; DEFAULT_PLANNER_PROMPT + sample prompts gained 2-step few-shot examples + JSON-only reminder after query; query no longer sent twice when systemPrompt contains {{QUERY}}.
2. evaluator.js/evaluation.js: errorRate = items with ok=false OR hasStepErrors; fallbackRate + avgF1Matched (rule-matched-only F1) surfaced with badges; scoreItem supports ordered:false (multiset) and alternatives (max-F1); param comparison domain-normalized (case/space, trailing '역', alphanumeric ID separators); model override now forced onto ALL strategies when chosen (UI hint updated); temperature-unify option added; leaderboard sort dropdown; insight explains F1 gap via precision vs recall; pre-run warning when benchmark references deleted MCPs; CSV formula-injection guard; run save failure → retry with 10 items → error toast.
3. store.js: import(obj,{includeAccounts=false}) with per-key type validation, account entry schema validation (all-or-nothing), recursive __proto__/constructor/prototype key removal; returns {applied,skipped,accountsIncluded}; persist failure dispatches 'rbtl:persist-failed' (app.js toasts, 5s throttle per key); 'storage' event listener syncs cache across tabs.
4. router.js: idempotent start(), new stop(); app.js: route-changed listener detach on re-shell, teardownShell() before login/setup render, auth check in router before-hook.
5. ui.js: modal close() idempotent + always removes ESC listener. auth.js: cannot delete own account; session TTL 7 days.
6. ollama.js: extractJSON iterates all fence blocks + all {/[ start positions (max 20); chatJSON returns calls:1|2; chat reads num_ctx from settings.numCtx (settings UI select added).
7. mockEngine.js: validateParams checks minimum/maximum, nested object required/types (1 level), array item types; executeTool(server,tool,params,{signal}) aborts pending waits; mock.samples shallow-validated against outputSchema else falls back to generated output.
8. New services/mcpUtils.js: shared normalizeServer/normalizeTool/validateSchema/sanitizeKeys etc. used by builder (manual+AI) and catalog import; MCP delete confirm shows referencing strategy-step/benchmark-item counts; catalog gained sample-restore button, tag chips capped at 2 (+N), mobile toolbar stacking; AI generation pre-checks Ollama connection; icon preview/입력 labels.
9. benchmarkGen.js/benchmarks.js: returns cancelled flag (wizard shows '중단됨 — 부분 결과 N개'); review screen amber warning banner (unverified LLM answers); difficulty reconciled with workflow length (1=easy/2-3=medium/4+=hard); focus servers round-robin coverage; manual editor '순서 무관 채점' checkbox (item.ordered=false) + table badge.
10. guide.js: new section '평가 결과 해석 주의사항' (mock randomness vs ReAct, unverified auto benchmarks, rule fallback separation, skill precision caveat, model/temp unification, num_ctx sensitivity).
11. main.css: contrast tokens raised (--tx2 #8093ab, new --tx2b #8c9db3, --tx3 #5a6d85 decorative only); .tbl min-width 640px (mobile h-scroll); input focus ring strengthened; toasts moved top-right below topbar; disabled buttons gray; .mcp-card h4 2-line clamp; placeholder brightened; schema-tbl th/.mcp-id/.acc-sub/.auth-note promoted.

Rejected (with rationale): constant-time hash compare (local-only, no practical threat); skill early-exit code change (sample design concern → documented in guide instead); auto-gen review default-unselect (kept selected for convenience; warning banner added instead); alternatives editor UI (scoring supports it via JSON import; editor UI deferred).
